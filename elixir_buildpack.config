# Versions come from .tool-versions, which mise reads too — one source of
# truth. Deliberately no `release=true` here: that flag belongs to the older
# distillery flow and conflicts with the releases buildpack.
always_rebuild=false
